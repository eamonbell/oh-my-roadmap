# /roadmap:resume

Read `.roadmaps/active.yml`, call `roadmap_engineer_read_state`, call `roadmap_engineer_render_report`, summarize the active state, relevant code/docs context, and propose the next legal action. Use the built-in `ask` tool if the next action depends on missing user decisions or approvals. Do not continue implementation automatically.
