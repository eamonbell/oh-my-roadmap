{
	"name":            "@oh-my-roadmap/cli",
	"version":         "0.9.0",
	"description":     "CLI for oh-my-roadmap: scaffold project config and (re)generate agent definitions from config models.",
	"type":            "module",
	"license":         "MIT",
	"bin":             {
		"omr-cli": "dist/index.js"
	},
	"files":           [
		"dist",
		"skills"
	],
	"scripts":         {
		"build": "rm -rf ./dist ./skills && cp -R ../core/skills ./skills && bun build ./src/index.ts --target node --outfile ./dist/index.js && chmod +x ./dist/index.js"
	},
	"devDependencies": {
		"oh-my-roadmap-core": "0.8.0"
	},
	"engines":         {
		"node": ">=18"
	},
	"publishConfig":   {
		"access": "public"
	}
}
