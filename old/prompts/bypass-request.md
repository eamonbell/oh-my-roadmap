# /bypass:request

Request a temporary bypass. Inspect the current state and blockers first. Use the built-in `ask` tool to interview the user until the bypass reason, scope, risk, and approval are explicit. A bypass always requires a reason and must be recorded with `roadmap_engineer_transition` using `request_bypass`.
