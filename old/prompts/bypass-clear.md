# /bypass:clear

Clear the active bypass with `roadmap_engineer_transition` using `clear_bypass`, then validate state. Use the built-in `ask` tool first if it is unclear whether the bypass risk has been resolved or the user wants the bypass removed.
