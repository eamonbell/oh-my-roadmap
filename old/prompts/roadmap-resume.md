# /roadmap:resume

Read `.roadmaps/active.yml`, call `roadmap_engineer_read_state`, call `roadmap_engineer_render_report`, use `roadmap_engineer_search_context` for relevant decisions, risks, notes, and blockers, summarize the active state and relevant code/docs context, and propose the next legal action from persisted structured state. For implementation resume, treat the progress cursor (`active_wave_id`, `step`, `active_task_ids`, and `blocked_reason`) as authoritative; notes are supporting context. If active worker runs are present, report them with their job/agent IDs, but do not poll or probe from `/roadmap:resume`. If the current session has no matching background job and no matching IRC peer for an active run, say that `/milestone:implement` should record that run abandoned before redispatching; do not wait. If blockers close the gate, show these exact recovery commands: `/blocker:list`, then `/blocker:resolve <id> <resolution>` or `/blocker:defer <id> <reason>`, then `/roadmap:resume`. Use `roadmap_engineer_read_context` only for selected entries that need full detail. Use the built-in `ask` tool if the next action depends on missing user decisions or approvals. Do not continue implementation automatically.
